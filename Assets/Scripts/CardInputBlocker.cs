using UnityEngine;
using UnityEngine.EventSystems;

public class CardInputBlocker : MonoBehaviour//, IPointerDownHandler, IPointerUpHandler, IEventSystemHandler
{
   /* public Card card;
    private bool isBlocked = false;

    private void Start()
    {
        card = GetComponent<Card>();
    }

    public void OnPointerDown(PointerEventData eventData)
    {
        if (!card.isZoomed)
        {
            isBlocked = true;
        }
    }

    public void OnPointerUp(PointerEventData eventData)
    {
        isBlocked = false;
    }

    public bool IsBlocked()
    {
        return isBlocked;
    }*/
}
