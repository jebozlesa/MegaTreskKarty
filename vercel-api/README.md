# Deprecated Mirror

This folder is no longer a deploy source of truth.

Use `c:\Zlozka\mega-tresk-server\api\` for all serverless endpoints and battle logic.

Deprecated files were intentionally replaced with loud stubs to prevent silent drift between repos.
